# Jarvis Todo Manager

## How it works

clone this repo
run `npm install`
run `npm link`

now type
`todo --help`

now add todos

and add it in auto script (which load automatically when cpu turn on)

`todo l`
